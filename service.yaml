apiVersion: v1
kind: Service
metadata:
  name: testapp-service
  namespace: default
spec:
  selector:
    app: testapp
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: MyApp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376

